import * as fx from 'money';

var conversor = function conversor(_ref) {
  var rates = _ref.rates,
      base = _ref.base;

  var fxInstance = fx.factory();
  fxInstance.rates = rates;
  fxInstance.base = base;

  return fxInstance;
};

export default conversor;